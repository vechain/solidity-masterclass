import { Config, WarningRule } from './type-extensions';
declare type NormalizedWarningRule = WarningRule & string;
interface SourceLocation {
    file: string;
    start: number;
    end: number;
}
export declare class WarningClassifier {
    private config;
    private rules;
    private ignoreRanges;
    constructor(config: Config);
    getWarningRule(errorCode: number | undefined, sourceLocation: SourceLocation): NormalizedWarningRule;
    reprocessFile(file: string, contents: string): void;
}
export {};
//# sourceMappingURL=index.d.ts.map